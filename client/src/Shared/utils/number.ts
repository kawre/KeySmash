// round number
export const r = (s: string) => Math.round(parseFloat(s));

// parse to number
export const f = (s: string) => parseFloat(s);

// number with 1 decimal
export const d1 = (s: string) => console.log(s);
